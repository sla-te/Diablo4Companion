{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "enabledPlugins": {},
  "extraKnownMarketplaces": {},
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|Grep",
        "hooks": [
          {
            "type": "command",
            "command": "g=$(command -v graphify || echo \"$HOME/.local/bin/graphify\"); if [ -x \"$g\" ]; then exec \"$g\" hook-guard search; fi; exit 0"
          }
        ]
      },
      {
        "matcher": "Read|Glob",
        "hooks": [
          {
            "type": "command",
            "command": "g=$(command -v graphify || echo \"$HOME/.local/bin/graphify\"); if [ -x \"$g\" ]; then exec \"$g\" hook-guard read; fi; exit 0"
          }
        ]
      }
    ]
  }
}